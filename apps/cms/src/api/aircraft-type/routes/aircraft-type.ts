// Auto-generates standard REST routes (GET, POST, PUT, DELETE) for this content type
import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::aircraft-type.aircraft-type');